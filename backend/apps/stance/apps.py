from django.apps import AppConfig

class StanceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.stance'
    verbose_name = 'M2 - Topic Selection & Stance Measurement'
