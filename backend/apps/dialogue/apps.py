from django.apps import AppConfig

class DialogueConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.dialogue'
    verbose_name = 'M4 - Real-time Dialogue Room'
