from django.urls import re_path

# TODO: M4 WebSocket consumers
# from .consumers import DialogueConsumer

websocket_urlpatterns = [
    # re_path(r'ws/dialogue/(?P<room_id>\w+)/$', DialogueConsumer.as_asgi()),
]
