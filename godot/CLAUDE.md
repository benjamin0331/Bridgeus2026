# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 語言

一律以**繁體中文**回答使用者。

## What this is

BridgeUs — a 2D social-RPG prototype in **Godot 4.7** (GL Compatibility renderer, d3d12 on Windows). Two players meet in a shared world, post a discussion 議題 (issue) above their head, walk up to each other, read each other's issue, and start a 1-on-1 chat. The whole flow runs on **Godot's own multiplayer** (`WebSocketMultiplayerPeer`, port 8085 — 8080 is taken by React/Docker on this same dev box); a Django backend integration is live behind a seam (see below).

## Running

No build/lint/test CLI — this is an editor-driven Godot project. Open it in the Godot 4.7 editor and press Play, or `godot --path .`. Main scene is `World/Game.tscn`.

To exercise multiplayer you need **two running instances**: one clicks **Host** (`create_server`), the other clicks **Join** (`create_client` to `127.0.0.1:8085`). In the editor: Debug → Run Multiple Instances → 2. There is no automated test harness despite the "multiplayer synchronization test" commit.

## Architecture — the load-bearing decisions

**All cross-peer RPCs live on the player node, never on the UI.** A player is named `str(peer_id)` and lives at `/root/Game/<peer_id>` — an *identical path on every peer*, which is what makes `rpc_id` targeting work. The UI (`UI/game_ui.gd`, registered in the `issue_ui` group) is a screen-space `CanvasLayer` that is **local-only and contains zero RPCs**; it calls into the local player for anything networked, and the player calls back into the UI (found via the `issue_ui` group) to display incoming events. Keep this split — moving an RPC onto the UI breaks path resolution across peers.

**Spawn flow (ticket-based identity, not client-declared):** `request_spawn(id)` is gone — the old version trusted whatever peer id the client sent, so a modified client could claim to be anyone. The replacement is `submit_ticket.rpc_id(1, ticket)`: the client's `_on_join_pressed` first (on Web only) awaits `Backend.request_entry_ticket()`, a coroutine that asks the **host page** (`GodotLobby.jsx`'s `window.bridgeus_request_ticket()`) for a one-time, 60s-TTL entry ticket and polls `window.bridgeus_ticket` for the result — pulled fresh before every connection attempt, not pushed at iframe load, because a pushed ticket wouldn't survive WASM cold-start or a reconnect. Once `connected_to_server` fires, the client sends that ticket via `submit_ticket`; the server never trusts a self-reported id — it always reads `get_remote_sender_id()`.

On the server, `submit_ticket` branches on `Backend.service_token`. **Local dev** (`service_token == ""`, i.e. a desktop host with no `GODOT_SERVICE_TOKEN` in the environment): no service key means no way to redeem tickets against the backend, so the server spawns the peer with no identity at all — pure local play. **Production** (dedicated server, service key present): the ticket is redeemed via `Backend.redeem_ticket()` (POST with `X-Godot-Service-Token`) into a real backend `user_id`; failure of any kind (invalid/expired/reused ticket, network error) is uniform — the caller just `disconnect_peer`s, no reason is distinguished because there's only one thing to do with it. On success the `user_id` is recorded in `game.gd`'s `_peer_users` — a `peer_id -> user_id` dict that is **server-only and never synced** (any client-writable synced property is a property a client can forge; see the seat-assignment note below). Only after this does `_spawn_player` run, `add_child`ing `player_00.tscn` named `str(id)` under `Game`; the `MultiplayerSpawner` (`spawn_path=".."` → `Game`) replicates it to everyone. Each player sets its own multiplayer authority from its name in `_enter_tree`; `is_multiplayer_authority()` gates input, camera, and proximity detection so only the local player is driven.

**Appearance = level (frog colour).** All looks live as animation pairs (`lvN_idle` / `lvN_run`) in the single player scene's one `AnimatedSprite2D` SpriteFrames — no per-character scene. `appearance` **is the player's level** (0–6): the authority sets it in `_ready` from `Backend.level` via `apply_level()`, and `game.gd::_apply_level_to_local_player` re-applies it when `GET /api/titles/me/` lands (the HTTP response and the Host/Join click race, so both paths are covered). Level count is derived by probing `lv0_idle`, `lv1_idle`, … until one is missing, so adding a level = add its two animations + its sprites, no code change. It probes sequentially rather than counting every `lv*_idle` because the SpriteFrames also holds `lv777_idle`/`lv777_run` — the **1-in-10 rare rainbow frog** rolled per spawn by `roll_appearance()`; pattern-counting would make it "level 8" and push `clampi`'s ceiling to a nonexistent `lv7_idle`. `777` is a sentinel chosen so `"lv%d_%s"` formats straight to the rainbow animation with no mapping code, and it rides the existing synchroniser so other peers see it too. `apply_level()` no-ops the colour when `is_rare()`, otherwise the `/titles/me/` response would overwrite the rare frog; the legend drops its `▸` marker for that session (`refresh_level_legend(false)`) because the player's frog belongs to no level. `appearance` + a `moving` bool are synced via the MultiplayerSynchronizer (`spawn=true`, so late joiners get the right colour); every peer's `_process` plays `lv{appearance}_{idle|run}` and `flip_h` (synced) handles left-facing. Sizing: `_update_anim` normalizes every character to `SPRITE_PX` tall via `scale = SPRITE_PX / frame_height`, applied **once per appearance** (tracked by `_scaled_for`, decoupled from idle/run switching — else the scale only landed on the first animation change and the sprite visibly resized when it started moving). With uniform-spec art (all frames the same px) this yields one scale for everyone.

The seven frog colours are **generated, not hand-drawn**: `scripts/recolor_frog.py` palette-swaps the three body shades of `Assets/ToxicFrog/GreenBlue` into `Assets/ToxicFrog/Level/Frog_LvN_{Idle,Hop}.png`. Read its header comments before touching the colours — the palette, the shadow derivation, and the hues that were rejected (and why) are all documented there, and it has a `--demo` self-check. The legacy `char_0..char_5` animations are still in the SpriteFrames but never played; they're kept so random appearance can be restored. Thresholds and the level→colour table live in `docs/godot_level_colors.md`.

**Issue broadcast & live refresh:** `submit_issue` → `apply_issue.rpc(...)` (`@rpc("authority","call_local","reliable")`) runs on every peer, sets `issue_title/_body`, updates the head bubble, and calls `ui.refresh_menu()` so a bystander already standing in proximity sees「（對方尚未提交議題）」flip to the real title without re-entering range. **Late joiners get issues too**: on connect, `game.gd` fires `request_issue_sync.rpc()`, and every authority re-sends its issue (and banner/reactions) to the requester via `apply_*.rpc_id`. This is the one late-join catch-up path — RPCs alone don't reach peers who join later.

**Issue reactions (表情回復):** while reading someone's issue, you pick **one** of 5 emoji (Read panel bottom-row buttons) — one reaction per reader per issue. The emojis live in `Globals/Emoji.gd` — a `class_name Emoji` static atlas table (16×16 grid) shared by buttons, head display, and the count column; `Emoji.tex(i)` / `Emoji.REGIONS`. Not an autoload (pure data). Flow mirrors issues: reader → `react_to_issue` → `add_reaction.rpc_id(author)` → **author holds one-per-reader, changeable** via `_reactor_ids` (a `{sender_id: idx}` dict keyed on `get_remote_sender_id()`, authority-only, not synced — reacting overwrites that sender's value; `reactions = _reactor_ids.values()` is derived from it, so a re-pick swaps the emoji in place without reordering the head row) → `apply_reactions.rpc(...)` broadcasts the whole idx list → every peer rebuilds the world-space head row (overlapping ~half via HBox `separation=-8`, no background, capped at `HEAD_MAX`=10 then「…」). Because the head is ambiguous at a glance, the Read panel's **right column** lists all 5 emoji with per-idx counts (`emoji ×N`), tallied from `_target.reactions` and refreshed live via `on_reactions_changed`. Reader-side, `_reacted_targets` tracks the reader's current pick per target; the chosen one shows a gray bottom bar and pressing another emoji re-picks (moves the bar, overwrites author-side). `apply_issue` clears both `reactions` and `_reactor_ids` (re-submit/delete wipes reactions) and calls `on_issue_reset` so readers' local locks clear too. Late joiners get reactions via `request_issue_sync`, sent **after** the issue (apply_issue clears, so order matters).

**Invite/chat handshake** is a chain of `@rpc("any_peer","reliable")` calls between the two players' nodes (`receive_invite` → `invite_result` → `send_chat`/`receive_chat` → `peer_left_chat`). Godot 4 `server_relay` (on by default) relays client↔client RPCs through the host, so host and client are symmetric.

**Voice call** (`Globals/VoiceChat.gd`, autoload) mirrors the chat handshake and is the project's second-largest subsystem, even though it's easy to miss reading only `game.gd`. `player_00.gd` has its own handshake chain — `invite_to_voice` → `receive_voice_invite` → `respond_voice_invite`/`voice_invite_result` → `leave_voice`/`peer_left_voice` — structurally identical to the chat invite chain above. Once accepted, `VoiceChat.start()` builds the DSP graph: microphone → a dynamically-created `VoiceMic` audio bus (there is no such bus in the scene tree beforehand; `AudioServer.add_bus` creates it at call time and `stop()` tears it down) → `AudioEffectPitchShift` (scale adjustable live from a UI slider, 0.5 deep voice ~ 2.0 chipmunk) → `AudioEffectCapture` (grabs post-effect frames for network transport) → `AudioEffectSpectrumAnalyzer` (feeds the UI's voice-activity bars). Capture and the analyzer are pinned at the end of the effect chain deliberately — both transport and visualization operate on the pitch-shifted audio, not the raw mic input. Every frame, the authority side pulls captured frames and ships them with `receive_voice.rpc_id(_voice_peer, frames)` — an unreliable RPC carrying a raw `PackedVector2Array` of PCM samples, played back on the receiving end through an `AudioStreamGenerator`.

**Host handoff is a race, and it was intermittent.** `GodotLobby.jsx::handleLoad` sets `window.bridgeus_token` / `bridgeus_api_base` / `bridgeus_ws_url` (and installs `bridgeus_request_ticket`) on the iframe's `load` event, but Godot's own boot can finish *first* when the browser has the wasm/pck cached — so `game.gd::_ready()` sometimes read them before they existed. Symptom: the level legend intermittently showed no 場次 and every frog was Lv0 white, because losing the race means no token, and `/titles/me/` is then never called at all. The worse variant is `bridgeus_ws_url`: losing the race silently connects to the local fallback address. `_ready()` now `await`s `_await_host_handoff()` (poll `window.bridgeus_token` every 50 ms, give up after 2 s) on Web before anything reads those variables, and re-runs `_resolve_connection_settings()` afterwards. UI wiring happens *before* the await so the screen stays responsive. Don't turn this back into a one-shot read.

**Screen-space panels must be anchored, not offset.** `[display]` uses `stretch/mode="canvas_items"` with `aspect="expand"`, so the visible rect is *wider* than the 1280×720 base on most windows. `GameUI.tscn`'s panels (Menu/Read/Invite/Chat/Voice) hardcode 1280-based offsets and therefore sit left of true centre on a wide window — the rare-frog popup originally copied that and had the same bug. It now anchors bottom-centre (`anchor_left/right = 0.5`, `anchor_top/bottom = 1.0`, offsets measured from there). Prefer that for anything new; the existing `.tscn` panels are unfixed.

**Movement lock while a window is open.** `player_00.gd::_physics_process` asks `game_ui.blocks_movement()` every frame, so that one function is the *only* place this rule lives — a new panel just gets added to its list. It currently covers Read / Invite / Chat / Voice / the rare-frog popup, plus two things that are not children of the UI node: the NPC dialogue box (via the `dialogue` group → `dialogue_box.gd::is_open()`) and the submit-issue form (via `suppress_menu`, which `game.gd::_set_menu_suppressed` raises while the form is up). The proximity **Menu is deliberately excluded**: that panel closes by the player *walking out of range*, so locking movement would leave it open forever and wedge the player. The separate `focus is LineEdit or TextEdit` check stays — it stops WASD leaking into text fields regardless of which panel owns them.

**Background music** is the `Bgm` autoload (`Globals/Bgm.gd`): one looping track (`Assets/Audio/retro-bgm-chan-home-at-night-516298.mp3`) at `-14 dB`, silenced to `-80 dB` for the duration of a voice call. The mute hook lives inside `VoiceChat.start()` / `stop()` rather than in the UI — those two are the *only* entry points to voice (`player_00.gd`'s `_start_voice`/`_stop_voice` both route through them), so no caller can forget to restore the volume. The track is fetched with `load()`, not `preload()`: `preload` resolves at parse time, so a not-yet-imported mp3 (no `.import` yet) would take the whole script down with a parse error. Loop is set in code (`stream.loop = true`) because Godot's mp3 importer defaults it off. Headless (the dedicated server) skips audio entirely. On Web the browser blocks autoplay until a user gesture — no special handling needed, sound starts when the player clicks Host/Join.

## Conventions that bit us (don't relitigate)

- **Collision layers:** players are on **layer 2, mask 1** so they pass *through* each other but still collide with the environment (`Boundry`/walls must stay on layer 1, "Environment"). Without this, `move_and_slide` overlap-recovery shoves the other player. The proximity `Area2D` uses `collision_mask = 2` and group-filters to `players` in its callback.
- **Head bubble** lives in *world space* under the player, so the 2× camera zoom doubles it: font is rasterized at 18px and the box scaled `BUBBLE_SCALE = 0.5` to land crisp 1:1 on screen. `BUBBLE_Y` is the vertical offset above the player origin — a hand-tuned constant (the sprite *frame* is 144px tall but the visible creature's head sits near y≈−10), adjust it in the editor by eye.

## Backend seam

`Globals/Backend.gd` is the single seam to the Django backend — every network-facing call routes through it via native `HTTPRequest`. `guest_login` is **gone** (removed along with the client-declared-identity model it enabled — see Spawn flow above); no anonymous, unowned accounts get created anymore. `submit_issue` and `request_topic_match` are **live**; `analyze_stance/_emotion` return null (intentionally unimplemented, nullable placeholders reserved). The backend is optional — no server = local-only play (issues still broadcast, just not persisted). In this monorepo the Django backend lives in `../backend/` (runs on port 8005); the `issues`/`godot` endpoints `Backend.gd` calls are wired in `../backend/api/urls.py`. The integration spec, endpoint gap-analysis, and web-deployment plan are maintained by the team as separate design docs outside this repo (`godot/.gitignore` excludes `docs/`, so no versioned copy lives here).

`BASE_URL` is now a `var`, not `const`: desktop dev keeps `http://localhost:8005/api`; Web builds default to same-origin `/api` (the whole stack — frontend, API, Godot static files, Godot WS — sits behind one Cloudflare-tunneled domain, path-routed) unless the host page sets `window.bridgeus_api_base` first. **Either way a leading-`/` base is expanded to an absolute URL with `window.location.origin`** — `HTTPRequest.request()` parses the URL itself and rejects anything without an `http(s)://` scheme (`ERR_INVALID_PARAMETER`), so the browser never gets a chance to resolve a relative path. This silently killed *every* API call in the Web build (level stuck at 0 = white frog) until it was fixed; `_ready()` now prints the resolved `BASE_URL` so the same class of bug shows up in the console immediately. `service_token` is a second, unrelated secret: read from the `GODOT_SERVICE_TOKEN` env var, **only ever populated on the headless dedicated server** (never in a Web export — `OS.has_feature("web")` is false there, and it's never set from `window.*`).

There are now **two separate identity channels**, and mixing them up is the easiest way to misread this codebase:
- `acquire_token_from_host()` reads `window.bridgeus_token` from the host page and sets `Backend.access_token` — a real login JWT the **client itself** uses to call `submit_issue`/`get_my_titles`/`set_my_title`/reaction endpoints directly. It only ever returns a token now, not a `user_id` — there is no `Backend.user_id` field; that field does not exist anymore.
- `request_entry_ticket()` (client-side coroutine, **must be awaited**) always suspends — it opens with an unconditional `await get_tree().process_frame` before branching into the Web polling loop or the desktop early-return, so desktop and Web both come back as a completion Signal, never a plain value. This is deliberate: two-instance desktop testing is this project's actual acceptance path, and if desktop returned synchronously while Web suspended, a missing `await` would compile, run fine in the desktop test everyone actually runs, and only fail — silently — once it shipped to Web. Making both paths suspend identically means the mistake fails the same way everywhere, including the desktop test. It asks the host page for a one-time entry ticket and hands it to the game server via `submit_ticket`, purely to prove which backend account is joining the multiplayer session. `redeem_ticket()` is the server-side counterpart (dedicated server only, requires `service_token`) that exchanges that ticket for a `user_id` by calling the backend.

`request_topic_match` POSTs to `/godot/match-rooms/` with an `X-Godot-Service-Token` header and the two players' **backend `user_id`s**, not Godot peer_ids — sourced from `game.gd`'s server-only `_peer_users` identity table (see `_do_seat`), never from anything synced on the player node. The backend endpoint now exists (`../backend/api/views.py` `GodotMatchRoomView`) and is idempotent: 201 creates a room, 200 means this pair already had an active room for this topic and it was reused, so a retry can't strand the two players in different rooms. Both status codes' response now also carries `topic_id`, which is what `match_found` forwards into the frontend route. Godot is not *unable* to produce that number — `Backend.gd`'s private `_TOPIC_ID_MAP` already maps slug→id on the way out — but `game.gd` deliberately reads it back off the response instead of reaching into that map or keeping a third copy: the backend is the authority on which room the pair belongs to, and the slug→id mapping is already duplicated once (`_TOPIC_ID_MAP` in `Backend.gd`, `_TOPIC_TRUNKS` in `game.gd`) without adding a third consumer.

**Match binding is asynchronous, all the way through** (`game.gd`, added after the original "fire the HTTP call and forget" version left players with no body and no buttons in an empty world regardless of whether the backend call succeeded): `_do_seat` fills the two trunks of a topic and hands off to `_try_start_match`, which re-derives `user_ids` from `_peer_users` (never from the player node), rejects a same-user match (`user_ids[0] == user_ids[1]` — two tabs from one login redeem two tickets into two peers but one `user_id`; the backend would 400 on this too, but blocking here lets the message name the actual problem instead of a generic failure), sets `_matching_topics[topic] = true`, then calls `Backend.request_topic_match(...)` with a bound callback. Nothing about the two seats is touched yet — they stay occupied while the HTTP round-trip is in flight.

`_matching_topics` is an in-flight flag per topic, not per seat. It's set when the HTTP request goes out; on the **success** path it is held until `_finish_match` has *finished its cleanup* — not merely until the response arrives — while each of the three failure branches releases it immediately (there is nothing left to protect once the pair has been unseated). `_do_seat` refuses new seat requests for a topic while its flag is set ("這個議題正在配對中，請稍候再試"). The reason it has to span that whole window: the two occupied seats are still "live" during the HTTP call (the waiting-panel cancel button works the entire time), so a third player sliding into a seat freed by a cancel would make the eventual callback's seat re-validation fail — and that failure would evict the *other*, still-legitimately-seated player along with the canceller.

The callback, `_on_match_room_created(code, data, topic, occupants)`, treats three cases as failure and none of them delete anyone silently: a non-2xx code; a 2xx with an empty `room_id` or `topic_id <= 0` (a stale deployment, a proxy eating the body, a contract change — HTTP succeeding is not sufficient to trust the payload); after that, seat validation fails. On any of these it `_seat_deny_and_unseat`s the affected peer(s) so they can retry, instead of running `_finish_match` and leaving them bodiless. Seat validation itself re-reads `_occupancy` for the topic's trunks at callback time and checks that both original `occupants` are still seated — a peer id is not a seat, and if the in-flight window let someone cancel or disconnect, the id in `occupants` may no longer correspond to anyone sitting down. If only one is still seated, that one gets unseated too rather than being shipped into a room alone. Only when both are confirmed still seated does the server call `match_found.rpc_id` (or invoke it locally for the host) and then run `_finish_match`, which — after the 1.5s toast delay — clears **only the two trunks these `peer_ids` actually occupy**, not the whole topic (a third player who took a freed trunk during the in-flight window must not have their seat wiped), frees the two player bodies, and only then erases the `_matching_topics` flag.

`match_found(topic_id, room_id)` is where the payoff of D1/topic_id lands: on Web, it `JavaScriptBridge`-evals a `window.parent.postMessage({type: "bridgeus_match", topic_id, room_id}, origin)` (current origin, not `'*'` — iframe and host page are same-origin by deployment topology). The host page, `GodotLobby.jsx`, listens for `message`, checks both `event.origin` and `event.source` (checking only one lets either a same-origin sibling frame or a malicious external window spoof the redirect), validates `topic_id` is a positive integer, and `navigate()`s to `/topic/<topic_id>?mode=match&from=godot`.

## Deployment (dedicated server)

A browser tab can't `create_server` — the Web export can only ever `create_client`. `game.gd::_is_dedicated_server()` detects the headless deployment target two ways (`--server` on the command line, matching the systemd `ExecStart` ops runs; or `DisplayServer.get_name() == "headless"` as a fallback that doesn't depend on any export preset) and short-circuits `_ready()` into `_start_dedicated_server()`: it opens the WebSocket server and returns *before* the UI wiring, banner setup, or `Backend` identity handshake run — a dedicated server represents no player and must never call `acquire_token_from_host`. A dedicated server that comes up **without** `GODOT_SERVICE_TOKEN` set refuses to start at all (`push_error` + `quit(1)`) rather than limping along unable to redeem tickets or build match rooms — a deploy with the env var missing should fail loudly at boot, not surface later as an inexplicable in-game rejection. On Web, `OS.has_feature("web")` hides the Host button (nothing left that could call `create_server` anyway) and leaves Join as the sole entry point. The connection address is resolved once in `_resolve_connection_settings()`: Web reads `window.bridgeus_ws_url` (falls back to the local desktop address if unset), desktop always uses the local default — replacing the old hardcoded `PORT`/`ADDRESS` constants (renamed `DEFAULT_PORT`/`DEFAULT_ADDRESS`, now only the fallback, not the only option).

**Local-dev seating looks like a bug but isn't:** a desktop host has no `GODOT_SERVICE_TOKEN`, so every peer that joins it spawns with no entry in `_peer_users` (see Spawn flow above). If two such peers sit on the two trunks of the same topic, `_do_seat` hands the pair to `_try_start_match`, which finds both `user_ids` are `0`, refuses to call `request_topic_match`, and **evicts both players from their seats** with `seat_denied("配對需要正式登入身份，請從主功能頁面進入")` (moved into `_try_start_match` as part of the phase-3 async-callback rework above; the behavior itself is unchanged from earlier phases). This is intentional — sending `[0, 0]` to the backend would either 400 silently or, worse, fabricate a match between two unrelated real accounts if the backend ever stopped validating — but it reads exactly like a two-player desktop test failing to pair up, and *will* get reported as a regression if a future contributor doesn't know why. The fix, for testing this path locally, is to run a real dedicated server with a valid service token, not to "fix" the eviction.

A ~25s any-peer heartbeat RPC (`_heartbeat`, unreliable, empty body) fires from `_process` whenever the multiplayer peer is connected, to keep Cloudflare's WebSocket idle timeout (~100s) from closing quiet connections. The interval is a fixed constant, not configurable — it only needs to comfortably undercut the platform's timeout, not track it precisely.

`_on_join_pressed`'s first `await` used to be a fixed `get_tree().create_timer(0.2).timeout` — guessing when the WebSocket handshake finished before firing the old `request_spawn`. That fixed wait is gone twice over: the connect step now awaits `Backend.request_entry_ticket()` (Web only, to get a ticket before even calling `create_client`), and the join itself waits on a one-shot `connected_to_server` signal before `submit_ticket.rpc_id(1, _ticket)` — real network latency (especially through a Cloudflare Tunnel hop) isn't guaranteed to fit in 200ms. The **second** 0.2s wait (after `submit_ticket`, before `request_issue_sync`, giving the `MultiplayerSpawner`'s initial replication a moment to land) is unchanged — it isn't a handshake wait and wasn't in scope for that fix.

## Style

GDScript with leading-underscore for private members/helpers. Code comments here carry real rationale — read them before changing collision masks, RPC modes, or the bubble scaling. Comments tagged `ponytail:` mark deliberate prototype shortcuts.

Note: the live NPC dialogue is `UI/dialogue_manager.tscn` (a `CanvasLayer` scripted by `UI/dialogue_box.gd`), instanced in `World/Game.tscn`. The old experimental dialogue/player variants have been removed.
